name: 5-min-cron
on:
  schedule:
      - cron: '*/5 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run: |
          curl --request POST \
          --url 'https://simplestatuslive-production.up.railway.app/api/check'
